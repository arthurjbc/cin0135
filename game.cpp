#include "arvoreCuscuz.hpp"
#include <iostream>

using namespace std;

int main(){
    int n, m;
    cout << "Tamanho (n):" << endl;
    cin >> n;
    cout << "Retirada maxima (m):" << endl;
    cin >> m;
}
    if (max<root->Val()){
        max=root->Val();
    }